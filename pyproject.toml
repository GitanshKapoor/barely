[project]
name = "barely-workspace"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[tool.uv.workspace]
members = ["packages/core", "packages/cli", "packages/api", "packages/worker", "plugins/*"]
