[project]
name = "barely-reporter-pdf"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "reportlab",
    "barely-core"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
barely-core = { workspace = true }
